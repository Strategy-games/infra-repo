apiVersion: v1
kind: Service
metadata:
  name: game-logic-api
  namespace: minecraft-debug
  labels:
    app: game-logic-api
spec:
  type: ClusterIP
  selector:
    app: game-logic-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
